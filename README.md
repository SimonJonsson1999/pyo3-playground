# pyo3-playground
In this repo i explore the pyo3 crate to create python extensions in rust

# To run..
To build the rust library and run using it run the following:
```bash
uv run maturin develop

uv run main.py
```
